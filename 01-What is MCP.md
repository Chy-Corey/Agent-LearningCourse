# Model Context Protocol

> [什么是模型上下文协议 (MCP)？ - Model Context Protocol - MCP 模型上下文协议](https://mcp-docs.cn/docs/getting-started/intro)
>
> [MCP (Model Context Protocol)，一篇就够了。 - 知乎](https://zhuanlan.zhihu.com/p/29001189476)
>
> [MCP 三大原语：Tools、Resources、Prompts | MCP Wiki | Learn Agent](https://learnagent.wiki/mcp/cards/mcp-primitives)

### 一、MCP 是什么

MCP (模型上下文协议) 是一个用于将 AI 应用程序连接到外部系统的开源标准。通过 MCP，诸如 Claude 或 ChatGPT 之类的 AI 应用程序可以连接到数据源（如本地文件、数据库）、工具（如搜索引擎、计算器）和工作流（如专业提示词）—— 使其能够访问关键信息并执行任务。你可以将 MCP 想象成 AI 应用程序的 USB-C 接口。正如 USB-C 提供了一种连接电子设备的标准化方式，MCP 提供了一种将 AI 应用程序连接到外部系统的标准化方式。

![mcp-simple-diagram](.\image\01\mcp-simple-diagram.png)

通过MCP可以实现：

- 智能体（Agents）可以访问您的 Google 日历和 Notion，充当更具个性化的 AI 助手。
- Claude Code 可以利用 Figma 设计稿生成一个完整的 Web 应用程序。
- 企业聊天机器人可以连接到组织内的多个数据库，让用户能够通过对话分析数据。
- AI 模型可以在 Blender 上创建 3D 设计，并使用 3D 打印机将其打印出来。

根据您在生态系统中所处的角色，MCP 可以带来一系列好处。

- **开发者**：在构建 AI 应用程序或智能体，或与其进行集成时，MCP 可以减少开发时间和复杂度。
- **AI 应用程序或智能体**：MCP 提供了对数据源、工具和应用生态系统的访问，这将增强其能力并改善最终用户体验。
- **最终用户**：MCP 催生出更强大的 AI 应用程序或智能体，它们可以访问您的数据并在必要时代表您采取行动。

简单来说，MCP规定了一个统一的协议，开发者在写代码时只要适配MCP协议，就不需要针对不同的AI工具重新开发。

### 二、MCP的项目架构

MCP不仅是一个协议，它是一个完整的技术项目，整个 MCP 生态具体包含了：

- [MCP 规范](https://mcp-docs.cn/specification/latest)：MCP 的技术规范，概述了客户端和服务端的实现要求。
- [MCP SDK（开发工具包）](https://mcp-docs.cn/docs/sdk)：实现 MCP 协议的不同编程语言 SDK。
- MCP 开发工具：用于开发 MCP 服务端和客户端的工具，包括 [MCP Inspector](https://github.com/modelcontextprotocol/inspector)
- [MCP 参考示例](https://github.com/modelcontextprotocol/servers)：MCP 服务端的参考实现示例。

在学习MCP的过程中，其实就是在学习这个项目架构中的内容。

### 三、MCP的概念

#### 1. 角色

MCP 采用**客户端-服务端架构**，其中 MCP 主机（Host）—— 如 [Claude Code](https://www.anthropic.com/claude-code) 或 [Claude Desktop](https://www.claude.ai/download) 等 AI 应用 —— 会建立与一个或多个 MCP 服务端的连接。Host通过为每个 MCP 服务端创建一个 MCP 客户端来实现这一点。每个 MCP 客户端与其对应的 MCP 服务端保持专用连接。使用 STDIO 传输的本地 MCP 服务端通常服务于单个 MCP 客户端，而使用可流式 HTTP 传输的远程 MCP 服务端通常会服务于多个 MCP 客户端。MCP 架构中的关键参与者包括：

- **MCP 主机 (Host)**：协调和管理一个或多个 MCP 客户端的 LLM 应用（不是LLM本身，而是可以调用LLM的应用）
- **MCP 客户端 (Client)**：主机与服务端的桥梁，负责消息路由、能力发现、协议协商。
- **MCP 服务端 (Server)**：提供外部数据、工具、提示模板等能力，如 API 调用、数据库访问。

**Server**是指提供上下文数据的程序，无论其运行在何处。MCP 服务端可以在本地或远程执行。例如，当 Claude Desktop 启动[文件系统服务端](https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem)时，该服务端由于使用 STDIO 传输而在同一台机器上本地运行，这通常被称为“本地”MCP 服务端。官方的 [Sentry MCP 服务端](https://docs.sentry.io/product/sentry-mcp/)运行在 Sentry 平台上，并使用可流式 HTTP 传输，这通常被称为“远程”MCP 服务端。

##### 举例

假设你正在使用 Claude Desktop (Host) 询问："我桌面上有哪些文档？"

1. **Host**：Claude Desktop 作为 Host，负责接收你的提问并与 Claude 模型交互。
2. **Client**：当 Claude 模型决定需要访问你的文件系统时，Host 中内置的 MCP Client 会被激活。这个 Client 负责与适当的 MCP Server 建立连接。
3. **Server**：在这个例子中，文件系统 MCP Server 会被调用。它负责执行实际的文件扫描操作，访问你的桌面目录，并返回找到的文档列表。

整个流程是这样的：你的问题 → Claude Desktop(Host) → Claude 模型 → 需要文件信息 → MCP Client 连接 → 文件系统 MCP Server → 执行操作 → 返回结果 → Claude 生成回答 → 显示在 Claude Desktop 上。

> 初次看到这个流程会产生疑问：LLM只有输出Text的能力，它是如何激活对应的Client的？
>
> Client 之所以能被激活，靠的是 Claude Desktop（即 Host/宿主）充当了一个**“监听者”和“执行者”**的角色。整个过程可以拆解为以下几个步骤：
>
> **第一步：模型“动嘴”不动手（输出特定格式的文本）**
>
> 当你问“我桌面上有哪些文档？”时，Claude Desktop 会先把你的问题，连同当前可用的 MCP 工具列表（比如“文件系统读取工具”）一起打包成提示词发给 Claude 模型。
> Claude 模型分析后，发现自己无法直接回答，于是它**输出一段符合特定规则的文本**。这段文本通常是一段结构化的 JSON 代码，看起来像这样：
> `{"tool_name": "read_files", "arguments": {"path": "desktop"}}`
> 这本质上就是模型在“发号施令”，但它只是把这串文字吐了出来。
>
> **第二步：主机（Host）监听并拦截指令**
>
> Claude Desktop（Host）一直在后台紧紧盯着 Claude 模型输出的每一个字。当它发现模型输出的内容不是普通的自然语言，而是符合 **MCP 协议规范的工具调用指令（即那串 JSON 代码）**时，它就会立刻**拦截**下来。
> Host 心里会想：“哦，模型现在不是想直接回复用户，而是想调用工具。”
>
> **第三步：主机激活 Client 去“动手”**
>
> 一旦 Host 确认了模型的意图，它就会立刻唤醒自己内部内置的 **MCP Client**。
> Host 会把模型刚刚输出的那串 JSON 指令交给 Client，并告诉它：“去，按照这个指令，找对应的 MCP Server 把活干了！”
>
> **第四步：Client 连接 Server 并带回结果**
>
> MCP Client 拿到指令后，就会通过标准协议（比如本地通信的 stdio）去连接负责文件系统的 **MCP Server**，并把“读取桌面”的具体要求传达过去。
> MCP Server 真正去你的硬盘里扫描文件，拿到文档列表后，把结果返回给 Client，Client 再原封不动地交回给 Host。
>
> **第五步：结果喂回给模型，生成最终回答**
>
> Host 拿到真正的文档列表后，会再次把这些数据打包，连同你最初的问题，第二次发给 Claude 模型。
> Claude 模型这次拿到了真实数据，就能顺理成章地用自然语言回复你：“你的桌面上有 XXX.docx、YYY.pdf 等文档。”

### 四、 协议层级

MCP 由两层组成

- **数据层**：定义基于 **JSON-RPC** 的客户端-服务端通信协议，包括生命周期管理以及工具、资源、提示词和通知等核心原语。
- **传输层**：定义支持客户端和服务端之间数据交换的通信机制和通道，包括特定传输方式的连接建立、消息分帧和授权。

从概念上讲，数据层是低层，而传输层是高层。

#### 1. 数据层

数据层实现了一个基于 [JSON-RPC 2.0](https://jsonrpc.org.cn/) 的交互协议，定义了消息结构和语义。该层包括

- **生命周期管理**：处理客户端和服务端之间的连接初始化、能力协商和连接终止
- **服务端特性**：使服务端能够提供核心功能，包括用于 AI 操作的“工具”、用于上下文数据的“资源”以及用于交互模板的“提示词”
- **客户端特性**：使服务端能够请求客户端从宿主 LLM 进行采样、引导用户输入以及向客户端记录日志消息
- **实用特性**：支持额外功能，如用于实时更新的“通知”和用于长时间运行操作的“进度跟踪”

#### 2. 传输层

传输层管理客户端和服务端之间的通信通道和认证。它负责处理 MCP 参与者之间的连接建立、消息分帧和安全通信。MCP 支持两种传输机制：

- **Stdio 传输**：使用标准输入/输出流进行同一机器上本地进程间的直接通信，提供无网络开销的最佳性能。
- **可流式 HTTP 传输**：使用 HTTP POST 发送客户端到服务端的请求，并结合可选的服务器发送事件 (SSE) 实现流式功能。此传输方式支持远程服务器通信，并支持标准 HTTP 认证方法，包括 Bearer 令牌、API 密钥和自定义标头。MCP 建议使用 OAuth 来获取身份验证令牌。

传输层将通信细节从协议层中抽象出来，使得相同的 JSON-RPC 2.0 消息格式可以在所有传输机制中使用。

### 五、数据层协议

MCP 的核心部分是定义 MCP 客户端和 MCP 服务端之间的架构和语义。这部分定义了开发者**从 MCP 服务端向 MCP 客户端共享上下文的方式**。MCP 使用 [JSON-RPC 2.0](https://jsonrpc.org.cn/) 作为其底层 RPC 协议。客户端和服务端相互发送请求并做出相应响应。在不需要响应时可以使用通知。

#### 1. 生命周期管理

MCP 是一个有状态协议需要生命周期管理。生命周期管理的目的是协商客户端和服务端都支持的能力 (Capabilities)。

#### 2. 原语（Primitives）

MCP 原语是 MCP 中最重要的概念，其实就是**客户端和服务端可以相互提供的内容**。这些原语指定了可以与 AI 应用共享的上下文信息类型以及可以执行的操作范围。

##### Server

MCP 定义了 服务端可以暴露的三种核心原语，**给客户端调用**：

- **工具 (Tools)**：AI 应用可以调用以执行操作的可执行函数（例如：文件操作、API 调用、数据库查询）
- **资源 (Resources)**：为 AI 应用提供上下文信息的数据源（例如：文件内容、数据库记录、API 响应）
- **提示词 (Prompts)**：帮助构建与语言模型交互的可重用模板（例如：系统提示词、少样本示例）

每种原语类型都有关联的发现 (`*/list`)、获取 (`*/get`) 以及在某些情况下的执行 (`tools/call`) 方法。MCP 客户端将使用 `*/list` 方法来发现可用的原语。例如，客户端可以先列出所有可用工具 (`tools/list`) 然后执行它们。这种设计允许列表是动态的。

##### Client

MCP 还定义了 *客户端* 可以暴露的原语。这些原语允许 **MCP 服务端**作者构建更丰富的交互。

- **采样 (Sampling)**：允许服务端请求客户端 AI 应用进行语言模型补全。当服务端作者想要访问语言模型，但希望保持模型独立且不愿在 MCP 服务端中包含语言模型 SDK 时，这非常有用。他们可以使用 `sampling/complete` 方法请求客户端 AI 应用进行语言模型补全。
- **引导 (Elicitation)**：允许服务端向用户请求额外信息。当服务端作者想要从用户那里获取更多信息或请求对某项操作的确认时，这非常有用。他们可以使用 `elicitation/request` 方法向用户请求额外信息。
- **日志记录 (Logging)**：使服务端能够向客户端发送日志消息，用于调试和监控目的。

有关客户端原语的更多详情，请参阅[客户端概念](https://mcp-docs.cn/docs/learn/client-concepts)。

除了服务端和客户端原语外，该协议还提供了一些横向实用原语，用于增强请求的执行：

- **任务 (Tasks，实验性)**：持久执行封装器，支持对 MCP 请求（如昂贵的计算、工作流自动化、批处理、多步操作）进行**延迟结果获取**和状态跟踪

##### 举例

还是之前的例子

> 123
>
> **第一阶段：幕后准备（Host 替模型“看菜单”）**
>
> 在你按下回车键之前，或者刚按下的那一瞬间，Claude Desktop（Host）已经在后台悄悄做了一件事：
>
> - 它通过 MCP Client 向文件系统服务端发送了 **`tools/list`（工具发现原语）**。
> - 文件系统服务端回复了一份“菜单”：“我有 `read_files` 这个工具，专门用来读取目录。”
> - Host 把这份“菜单”悄悄记了下来，准备一会儿展示给大模型看。
>
> **第二步：模型“动嘴”下指令（输出特定格式的文本）**
>
> 当你问出“我桌面上有哪些文档？”时，Host 把你的问题连同刚才那份“菜单”一起打包发给了 Claude 模型。
> Claude 模型一看：“哦，虽然我看不到桌面，但我发现菜单里有个叫 `read_files` 的工具正好能解决这个问题。”
> 于是，它决定调用原语，并输出了一段符合规则的 JSON 文本：
> `{"tool_name": "read_files", "arguments": {"path": "desktop"}}`
> **（这一步，就是模型在决定使用哪个工具原语，并填好参数。）**
>
> **第三步：Host 拦截并激活 Client（翻译暗号）**
>
> Claude Desktop（Host）一直在监听模型的输出。当它发现模型吐出的不是普通回复，而是一串标准的 JSON 时，立刻意识到：“模型想调用 **`tools/call`（工具调用原语）** 了！”
> 于是，Host 激活内部的 MCP Client，把这串 JSON 指令交给它，并说：“去，用 `tools/call` 的格式，把这个指令传给文件系统服务端！”
>
> **第四步：Client 连接 Server 并带回结果（执行原语）**
>
> MCP Client 拿着指令，通过标准协议（比如 stdio）敲开了文件系统服务端（Server）的门。
>
> - Client 对 Server 说：“Host 让我执行 **`tools/call`**，调用 `read_files` 工具，参数是 `desktop`。”
> - Server 收到指令，立刻去你的硬盘里扫描桌面，拿到了文档列表（比如 `report.docx`）。
> - Server 把结果打包，原路返回给 Client，Client 再交给 Host。
>
> **第五步：结果喂回给模型，生成最终回答**
>
> Host 拿到了真实的文档列表，把它作为“上下文”，连同你最初的问题，第二次发给了 Claude 模型。
> Claude 模型这次有了真实数据，终于可以用自然语言回复你：“你的桌面上有 report.docx 这个文档。”

#### 3. 通知

该协议支持实时通知，以实现服务端和客户端之间的动态更新。例如，当服务端的可用工具发生变化时——比如新功能上线或现有工具被修改——**服务端可以发送工具更新通知，以告知已连接的客户端这些变化**。通知以 JSON-RPC 2.0 通知消息的形式发送（不期望响应），并使 MCP 服务端能够向已连接的客户端提供实时更新。
